<source>
  @type tail
  # The format across lines is not consistent, can not deduce structure.
  format none
  path /var/log/redis*.log,/var/log/redis/*.log
  pos_file /var/lib/google-fluentd/pos/redis.pos
  read_from_head true
  tag redis
</source>
